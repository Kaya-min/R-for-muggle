# 특징
1. 행(row) - 한 사람의 응답 값 (row 수: 사람 수)
2. 열(column) - 변수 (이 중에 독립변수가 있음)
3. 수준(level): 독립 변수의 가짓수
* 수준(level): 요인의 수준. 즉, 각 집단을 의미함. 예) 요인이 "성별"일 때, 수준은 "남", "여"
4. 독립변수: 이산형/범주형 데이터. 요인(factor)라고도 부름. 독립 변수의 갯수에 따라 one-way / two-way anova 중 어떤 분석 방법으로 진행할 지 선택하게 됨.
* 요인(factor): 집단을 구별하는 (독립)변수를 분산분석의 맥락에서는 "요인"이라고 칭함. 예) 성별, 국가
5. 종속변수는 연속형 변수여야함!
* 상호작용: 한 요인의 수준에 따른 종속변수의 차이가 또 다른 요인의 수준에 따라 달라질 때, "요인들 간 상호작용이 존재한다"고 함

# 가정
1. 독립성(independence of observation assumption) 
2. 정규성(normality assumption)
3. 등분산성(homogeneity of variance assumption)

# 종류
(1) One Way ANOVA (일원 배치 분산분석)
    : 독립변수가 1개이고, level이 3개 이상인 경우 각 그룹 간 유의한 차이가 있는지 검정한다.
    하나의 독립변수(수준은 2개)를 이용한 분산분석
    
(2) Repeated Measures ANOVA (반복측정 분산분석)
    : 독립변수가 1개이고 두번 이상 반복 측정된 샘플의 변화량에 유의미한 차이가 있는지 검정한다.

(3) Two Way ANOVA (이원 배치 분산분석)
    : 독립변수가 2개인 경우 사용한다.

(4) Two Way Repeated Measures ANOVA (이원 반복측정 분산분석)
    : 독립변수가 2개이고 두번 이상 반복 측정된 샘플의 변화량에 유의미한 차이가 있는지 검정한다.


# 사후검정 (post-hoc)
ANOVA검정은 차이가 있는 그룹이 있다는 것만 알려줄 뿐, 어떤 그룹들이 차이가 있는지는 알려주지 않는다.
따라서 유의미한 차이가 있다고 나온 경우 사후검정을 통해 어떤 그룹 간에 차이가 있는지 확인해야한다. (※ ANOVA 결과 차이가 없다고 나오면 사후검정은 필요없다.)
- Duncan Test
- Tukey's Test

# 참고: https://sysiphe0.tistory.com/5
# 참고: https://chukycheese.github.io/statistics/anova/ (정말 자세하뮤ㅠ 꼭 봐)
